{
  "name": "bowling-tool",
  "version": "1.0.0",
  "description": "Mobile bowling score tracker with live max-possible score and shared games",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
